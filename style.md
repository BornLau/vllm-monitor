# 推理集群监控面板 · 前端风格复现指南

## 一段式总结（可直接投喂给 AI 或交给设计师）

整套 UI 是典型的"企业内网数据监控面板"风格：克制、正式、信息层级清晰，无任何花哨装饰。整体气质由一个深藏蓝主色定调——页面背景为极浅的灰蓝 #f5f7fa，所有内容装在白色圆角卡片里（圆角 12px，配 0 2px 8px rgba(0,0,0,0.08) 的低强度柔和投影），内容区最大宽度 1400px 居中。顶部是通栏深蓝渐变 Header（linear-gradient 135deg，从更深的 #1a365d 渐变到主色 #2c5282），白色标题文字特意使用宋体（SimSun/宋体，26px，加 2px 字距）以突出"官方系统"的严肃感，副标题为小号半透明白字；Header 右侧是胶囊形分段切换器，底色为 rgba(255,255,255,0.15) 的毛玻璃效果，圆角 24px，激活项为纯白底配深蓝字 #1a365d。Header 下方是一条白底工具栏，左侧排列浅灰胶囊 Tab（#f0f0f0 底、20px 圆角，激活态变为深蓝 #2c5282 底白字），右侧是日期时间筛选控件。卡片内部结构统一为"卡片头 + 图表体"：卡片头左侧 16px/600 的深灰标题（#333），右侧 12px 浅灰说明文字（#888/#999），下方以 1px #f0f0f0 细线分隔；多个图表用 CSS Grid 排布，主图为单列、次级指标为 3 列六宫格（响应式：1200px 以下 2 列、768px 以下 1 列）或 2 列网格。章节标题带 4px 深蓝左侧竖条。图表用 ECharts 渲染，配色直接沿用 ECharts 默认色板：蓝 #5470c6、绿 #91cc75、黄 #fac858、红 #ee6666、浅蓝 #73c0de、紫 #9b59b6；双 Y 轴图表的轴线与轴标签颜色与系列同色。文字色阶非常工整：正文 #333 → 次要 #666 → 提示 #888/#999 → 表单正文 #2d3748 → 辅助说明 #718096/#a0aec0。交互控件统一走"深蓝主色 + 浅灰底"路线：主按钮 #2c5282 底白字（hover 加深至 #1a365d），次按钮 #edf2f7 浅灰底配 #2d3748 深灰字，危险操作为红系（文字 #c53030、浅红底 #fed7d7）；输入框为 1px #cbd5e0 边框、8px 圆角，聚焦时边框变 #2c5282 并加 3px rgba(44,82,130,0.12) 柔光；另有 iOS 风格开关（42×24px，关闭 #cbd5e0、开启 #2c5282）和居中模态框（全屏 rgba(0,0,0,0.45) 遮罩、白盒 12px 圆角，编辑类弹窗头部同样使用 #1a365d→#2c5282 深蓝渐变）。排名徽章使用金 #ffd700 / 银 #c0c0c0 / 铜 #cd7f32，普通名次为 #e8edf3 底配 #5a6a7e 字。字体为系统无衬线栈（-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif），仅主标题用宋体、地址等代码字段用 'SF Mono'/Menlo/Consolas 等宽字体；所有交互过渡 0.2–0.3s。整个配色体系（#1a365d/#2c5282/#2d3748/#4a5568/#718096/#a0aec0/#cbd5e0/#e2e8f0/#edf2f7/#ebf4ff/#c53030/#fed7d7 等）恰好对应 Chakra UI 旧版默认色板的 blue-800/900 与 gray 灰阶、red 系，复现时可直接引用该色板。

## 快速参考

### 配色表

| 用途 | 色值 |

|---|---|

| 主色（激活/主按钮/聚焦） | `#2c5282` |

| 主色 hover / Header 渐变深端 | `#1a365d` |

| 页面背景 | `#f5f7fa` |

| 卡片背景 | `#ffffff`（次级 `#fafbfc`、`#f8fafc`） |

| 正文 | `#333333` |

| 次要文字 | `#666666` |

| 提示文字 | `#888888` / `#999999` |

| 表单/深灰字 | `#2d3748` / `#4a5568` |

| 辅助说明 | `#718096` / `#a0aec0` |

| 输入框边框 | `#cbd5e0`（轻分隔线 `#f0f0f0` / `#eee` / `#e2e8f0`） |

| 浅蓝标签底 | `#ebf4ff`（配 `#2c5282` 字） |

| 绿色标签 | `#f0fff4` 底 + `#276749` 字 |

| 危险红 | 字 `#c53030`，底 `#fed7d7` / `#fff5f5` |

| 次要按钮 | 底 `#edf2f7`，字 `#2d3748` |

| 排名金银铜 | `#ffd700` / `#c0c0c0` / `#cd7f32`（普通 `#e8edf3` 底 + `#5a6a7e` 字） |

| ECharts 色板 | `#5470c6` `#91cc75` `#fac858` `#ee6666` `#73c0de` `#9b59b6` |

### 关键尺寸

- 内容区：`max-width: 1400px; margin: 0 auto; padding: 20px`

- 卡片：白底 + `border-radius: 12px` + `box-shadow: 0 2px 8px rgba(0,0,0,0.08)`

- 主图表高度 350px，小图 280px，模型小卡图 220px

- 胶囊 Tab：`border-radius: 20px`，激活 `#2c5282` 底白字

- 网格：3 列（gap 15px）/ 2 列（gap 20px），断点 1200px / 900px / 768px

- Header：`linear-gradient(135deg, #1a365d 0%, #2c5282 100%)`，白字

- 字体栈：`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif`；标题宋体 `SimSun/宋体`；等宽 `'SF Mono', Menlo, Consolas`

- 过渡：`transition: all 0.2s~0.3s`

---

# 页面布局

单 HTML 内含三个"虚拟页"（`#page-realtime` / `#page-stats` 两个页面 + 3 个管理弹窗），通过 `display:none` 切换，共享同一个 `.header`。

## 总体骨架

```

<body>

├─ .header ← 全站通栏，深蓝渐变，固定不变

├─ #admin-login-modal ← 管理员登录弹窗（默认隐藏，340px 宽）

├─ #admin-panel-modal ← 模型管理面板弹窗（min(920px, 92vw)）

├─ #admin-edit-modal ← 模型新增/编辑弹窗（min(560px, 94vw)）

├─ #page-realtime ← 实时监控页

│ ├─ .tabs ← 时间范围工具栏

│ └─ .container ← 1400px 居中内容区（全部图表卡片）

└─ #page-stats ← 数据统计页（默认隐藏）

├─ .tabs ← 粒度工具栏

└─ .container

```

## 实时监控页（主体页面）

```

┌──────────────────────────────────────────────────────────────────────┐

│ HEADER（通栏深蓝渐变，padding 20px 30px，flex 两端对齐） │

│ 左：标题（宋体 26px 白字）+ 副标题（14px 半透明白） │

│ 右：胶囊分段器 (圆角24px 毛玻璃底) │

│ ( 实时监控 )( 数据统计 )( 管理配置 ) ← 激活项白底深蓝字 │

├──────────────────────────────────────────────────────────────────────┤

│ TABS 工具栏（白底通栏，border-bottom，flex，可换行） │

│ 左：(1 Hour)(4 Hour)(8 Hour)(24 Hour)(All) ← 胶囊Tab，激活深蓝底 │

│ 右：开始 [日期▾][时▾]:[分▾] 结束 [日期▾][时▾]:[分▾] ☑最新 [查询] │

└──────────────────────────────────────────────────────────────────────┘

┌─ .container（max-width 1400px，居中，padding 20px）──────────────────┐

│ │

│ ▣ 卡片① 集群运行/排队总量 （全宽 · 图高 350px · 折线图） │

│ ▣ 卡片② 各模型排队情况 （全宽 · 图高 350px） │

│ │

│ ▣┌─────────────────────────────┐ ▣┌─────────────────────────────┐ │

│ │ ③ 系统请求吞吐量 (280px) │ │ ④ 系统Token吞吐量 (280px) │ │

│ └─────────────────────────────┘ └─────────────────────────────┘ │

│ ┌─────────────────────────────┐ ┌─────────────────────────────┐ │

│ │ ⑤ 模型请求量排行 (横向条形) │ │ ⑥ 模型Token量排行 (横向条形)│ │

│ └─────────────────────────────┘ └─────────────────────────────┘ │

│ ┌─────────────────────────────┐ ┌─────────────────────────────┐ │

│ │ ⑦ 模型请求增长曲线 │ │ ⑧ 模型Token增长曲线 │ │

│ │ 右上角: [选择模型 ▼] │ │ 右上角: [选择模型 ▼] │ │

│ └─────────────────────────────┘ └─────────────────────────────┘ │

│ ┌─────────────────────────────┐ ┌─────────────────────────────┐ │

│ │ ⑨ 前缀缓存命中率 [选择模型▼]│ │ ⑩ 外部前缀缓存命中率 │ │

│ │ │ │ [分母切换][选择模型 ▼] │ │

│ └─────────────────────────────┘ └─────────────────────────────┘ │

│ │

│ ▣ 卡片⑪ 总体缓存命中率（全宽，右上角模型下拉） │

│ │

│ ┌─────────────────────────────┐ ┌─────────────────────────────┐ │

│ │ ⑫ 后端模型TPS（排行榜列表） │ │ ⑬ 聚合TPS（排行榜列表） │ │

│ │ ① 金 ② 银 ③ 铜 徽章 │ │ 同左 │ │

│ └─────────────────────────────┘ └─────────────────────────────┘ │

│ │

│ ▣ 模型选择器（白卡：标题"选择模型查看详细指标" + 可换行胶囊Tab） │

│ │

│ ▣ #model-detail 模型详情（选中模型后动态生成） │

│ ┌─ .model-section（白卡 padding 20px）──────────────────────────┐ │

│ │ ▎模型名标题（18px，左侧 4px 深蓝竖条） │ │

│ │ ┌─────────── 六/八宫格 six-grid（3列, gap 15px）───────────┐ │ │

│ │ │ TTFT/TPOT │ TPS(脚本) │ 运行/排队 │ │ │

│ │ │ Token消耗 │ 请求吞吐量 │ Token吞吐量 │ │ │

│ │ │ 缓存命中率 │ TPS时序图 │ (图高 220px, 浅灰底 #fafbfc) │ │ │

│ │ └──────────────────────────────────────────────────────────┘ │ │

│ │ 子标题：vLLM服务端统计数据（按Engine）（底边框 2px #e0e0e0） │ │

│ │ ┌─────────── 直方图 histogram-grid（3列）────────────────┐ │ │

│ │ │ TTFT │ TPOT │ E2E Latency │ │ │

│ │ │ Queue Time │ Prefill Time │ Decode Time │ │ │

│ │ └───────────────────────────────────────────────────────┘ │ │

│ └──────────────────────────────────────────────────────────────┘ │

└──────────────────────────────────────────────────────────────────────┘

```

## 数据统计页

```

├─ TABS 工具栏：( 按天 )( 按周 )( 按月 ) ····· 右侧灰色说明文字

└─ .container

┌───────────── two-col ─────────────┐

│ ⑴ 集群请求处理总数 │ ⑵ 集群Token吞吐总量 │ ← 图高 350px

└───────────────────────────────────┘

▣ ⑶ 分模型请求处理个数（全宽，堆叠柱状图，350px）

▣ ⑷ 分模型Token吞吐量（全宽，堆叠柱状图，350px）

```

## 管理弹窗（三个，均为全屏遮罩 + 居中白盒）

```

遮罩 rgba(0,0,0,0.45)

┌─ 登录框 (340px) ──────────────┐

│ 标题"管理员登录" │

│ [密码输入框 ] │

│ 错误提示行 │

│ ( 取消 )( 登录 ) │

└─────────────────────────────┘

┌─ 管理面板 min(920px, 92vw)，max-height 85vh ─────────────────┐

│ 标题"监控模型管理" ( 新增模型 )( 退出登录 )( 关闭 ) │

│ 灰色 hint：修改直接写回 models.yaml... │

│ ┌─ 模型卡片列表（可滚动）───────────────────────────────┐ │

│ │ [模型名 | 类型标签 | 地址(mono) | ( 编辑 )( 删除 )] ×N │ │

│ └───────────────────────────────────────────────────────┘ │

└──────────────────────────────────────────────────────────────┘

┌─ 编辑弹窗 min(560px, 94vw) ─────────────────────────────────┐

│ ▓▓ 深蓝渐变头：标题 + 副标题（白字）▓▓ │

│ 表单体（可滚动，max-height 62vh）： │

│ 字段组：图标+标签+[输入框]（聚焦蓝色柔光） │

│ 类型选择卡片：两张可选卡（选中蓝边浅蓝底） │

│ verify_ssl 开关行（iOS 风格 toggle） │

│ 错误提示条（浅红底） │

│ ▓ 底部 #f8fafc：( 取消 )( 保存 ) ▓ │

└──────────────────────────────────────────────────────────────┘

```

## 布局要点速记

- **层级**：通栏 Header → 通栏工具栏 → 1400px 居中容器；容器内全是白卡，卡片间距 20px（六宫格/直方图网格内 15px）

- **卡片只有两种**：`.chart-card`（图表卡：头 + 图）和 `.tps-ranking-card`（列表卡：标题 + hint + 图例 + 列表）

- **网格**：主指标全宽单列；对比类指标一律 `two-col`（1fr 1fr，900px 以下折为 1 列）；模型详情用 3 列网格（1200px→2 列，768px→1 列）

- **图表卡头**永远三段式：左标题、右 hint（或筛选控件），下压 1px `#f0f0f0` 分隔线

- **筛选控件**（模型多选下拉、分母切换按钮）挂在卡片头右侧，不占图表区

- 所有交互都是"整页无刷新"：顶部 Tab 切时间、胶囊切页、弹窗管理配置，靠 fetch + ECharts 重绘
